from setuptools import setup

setup(
    name="StellarisUtils",
    version="0.1.0",
    packages=[
        # "stellaris",
        # "stellaris.model",
        # "stellaris.render"
    ],
    install_requires=[
        "Pillow",
        "numpy",
        "scipy"
    ]
)